{
  "name": "code-tutoring",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tailwind": "npx tailwindcss -i ./styles/tailwind.config.css -o ./styles/tailwind.css --watch"
  },
  "author": "",
  "license": "ISC",
  "description": "In the working directory, run `npm install`, then `npm run tailwind`",
  "devDependencies": {
    "tailwindcss": "^3.4.6"
  }
}
